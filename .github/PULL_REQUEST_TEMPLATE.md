<!--
Thanks for contributing to CodeLeWM. Please keep every section below;
leave a section as "N/A" if it does not apply rather than deleting it,
so reviewers can see the field was considered.
-->

## Summary

<!--
One paragraph: what changes, why now, and what user-visible behavior is
different. No marketing language.
-->

## Linked Issue

Closes #

## Spec / RFC Reference

- Spec section: `docs/spec/...`
- RFC: `docs/rfcs/...` (or `N/A` for non-spec-derived work)

## Public Surface Impact

<!--
List every public CLI flag, JSON schema, manifest field, error type,
config key, or Python API that this PR adds, changes, or removes.
Note "schema version bumps" explicitly. Write `none` if the PR does
not touch the public surface.
-->

## Validation

<!--
Copy the exact commands you ran locally and the result. CI runs the
same suite; differences are bugs in the workflow.
-->

```bash
python -m pytest tests/
```

## Artifact Impact

<!--
Does this PR change a manifest, report, log, or dataset on disk?
List affected schemas and any migration notes. Write `none` if there
is no on-disk impact.
-->

## Deprecations

<!--
List any public surface that is now deprecated by this PR. Each entry
must point at the replacement and link the upcoming removal release.
Reference `docs/spec/09-release-and-versioning.md`. Write `none` if
this PR does not deprecate anything.
-->

## Caveats / Follow-ups

<!--
Known limitations, residual risk, intentional gaps, or follow-up
issues. Write `none` if there are no caveats.
-->
